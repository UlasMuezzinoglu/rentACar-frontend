import { TestdateformatPipe } from './testdateformat.pipe';

describe('TestdateformatPipe', () => {
  it('create an instance', () => {
    const pipe = new TestdateformatPipe();
    expect(pipe).toBeTruthy();
  });
});
