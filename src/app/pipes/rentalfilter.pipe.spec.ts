import { RentalfilterPipe } from './rentalfilter.pipe';

describe('RentalfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RentalfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
